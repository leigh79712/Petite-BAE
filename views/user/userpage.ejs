<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.2/css/all.min.css"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@100;400;700&display=swap"
      rel="stylesheet"
    />
    <link rel="shortcut icon" type="image/png" href="/pic/logo.png" />
    <script defer src="/javascript/script.js"></script>
    <title>Petite BAE</title>
  </head>
  <body>
    <%- partial("./partial/navbar") %>
    <div class="container mt-5">
      <div class="row">
        <%- partial("./partial/userpagesidebar") %>
        <div class="col">
          <div class="card">
            <h5 class="card-header"><%= user.username %></h5>
            <div class="card-body">
              <h5 class="card-title">
                name : <%= String(user.firstname).toUpperCase().slice(0,1) +
                String(user.firstname).slice(1) %> ,<%=
                String(user.lastname).toUpperCase().slice(0,1) +
                String(user.lastname).slice(1) %>
              </h5>
              <p class="card-text">Email: <%= user.email %></p>
              <p class="card-text">Birthday: <%= user.birthday %></p>
              <a href="#" class="btn btn-sm btn-outline-primary float-end"
                >Edit</a
              >
            </div>
          </div>
        </div>
      </div>
    </div>

    <%- partial("./partial/footer") %>
  </body>
</html>
